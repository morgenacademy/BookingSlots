{
  "name": "bookingslots",
  "private": true,
  "version": "0.0.1",
  "description": "Booking + payment platform for boutique fitness studios. Bsport alternative.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter @bookingslots/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "supabase:reset": "supabase db reset"
  },
  "engines": {
    "node": ">=20.11"
  }
}
